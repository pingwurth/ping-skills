运行需先执行 `scripts/batch_diff.py` 生成候选清单，再运行 `scripts/batch_init.py --project-root <workdir>` 一次性 install+mvn 产出基线 `state.json` 与 `batch_state.json`；后续由 `batch_next.py` 逐类委派子代理循环执行 `make_plan→build_prompt→write_code→validate_rules→verify_coverage`；可通过环境变量 `JAVA_UT_MVN_TIMEOUT` 与 `JAVA_UT_BATCH_CLASS_ROUND_BUDGET` 覆盖 mvn 超时与类级预算。