每个目标类需指定独立 `--workdir`（默认 `<project-root>/.agent/java-unit-test-generator/`），通过 `state.json` 断点续跑；Maven 超时默认 1800s 可通过 `JAVA_UT_MVN_TIMEOUT` 覆盖；单方法/全局预算分别由 `JAVA_UT_METHOD_ROUND_BUDGET`、`JAVA_UT_GLOBAL_ROUND_BUDGET` 控制；覆盖率排除通过 `--coverage-exclude` 传入。