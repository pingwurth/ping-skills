完整流水线命令见 `CLAUDE.md`：需先由 LLM 生成并通过 schema 校验的 `sensors.json`，再依次运行 `extract_entries.py --audit`（可选）、`locate_call_sites.py`（要求 `entry.txt` 含 `# CONFIRMED`）、LLM 写 `sensors.csv`、`check_report.py`、`review_report.py`、`report_manager.py`。所有脚本默认把详细日志写入 `<out>.log`，stdout 仅输出 `next_step: ...` 一行提示下一步操作。