✔ 单元测试 skill 执行 mvn 的时候不要指定 -T 
✔ select_worktree.py 新增一个选项“清理历史工作树并创建新的工作树”(--clear-history)
3. 执行 select_worktree.py 创建工作树前先检查 .codegraph 目录是否存在，如果 .codegraph 目录不存在就中断任务，提示用户执行 codegraph init
4. 执行 select_worktree.py 创建工作树前先检查 `opencode mcp list | grep "codegraph.*connected"`, 如果不存在就中断任务，提示用户执行 codegraph install 配置 mcp
5. select_worktree.py 创建完工作树目录，将 .codegraph 拷贝到工作树的工程根目录下，并执行 `codegraph sync`
6. 新增飞速模式，一次生成即结束
7. 新增疾驰模式，编译验证通过即结束
8. 单元测试 skill 加载完第一件事 —— 让用户选择模式：飞速、疾驰、龟速
9. 超过预算轮数不再人工决策，检索 `references/単测编写指导.md` ，如果没有匹配方案直接跳过，state.json 记录跳过原因，并提醒用户更新 `単测编写指导.md`，如果找到匹配方案就追加 10 轮预算
10. 新增 junit4 支持
